import { Raleway } from 'next/font/google';

export const raleway = Raleway({
    weight: ["400", "500", "600", "700"],
    style: ['normal', 'italic'],
    variable: '--raleway',
    subsets: ['latin']
})
